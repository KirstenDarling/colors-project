'use strict';

var worker_threads = require('worker_threads');
var getCliConfig = require('../_chunks/getCliConfig-3f875ac6.js');
getCliConfig.getCliConfig(worker_threads.workerData, {
  forked: false
}).then(config => {
  var _a;
  return (_a = worker_threads.parentPort) == null ? void 0 : _a.postMessage({
    type: "config",
    config
  });
}).catch(error => {
  var _a;
  return (_a = worker_threads.parentPort) == null ? void 0 : _a.postMessage({
    type: "error",
    error: error instanceof Error ? error.stack : error,
    errorType: error && (error.type || error.name)
  });
});
//# sourceMappingURL=getCliConfig.js.map
